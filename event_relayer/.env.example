# Event Relayer Deployment Configuration
# Copy this file to .env and fill in the values

# Account credentials for deployment
# WARNING: Never commit actual private keys to version control
export DOJO_ACCOUNT_ADDRESS="0x..."
export DOJO_PRIVATE_KEY="0x..."

# Budokan contract address (set after Budokan deployment)
# Used for post-deployment configuration
export BUDOKAN_ADDRESS="0x..."
