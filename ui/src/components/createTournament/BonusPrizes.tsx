import { StepProps } from "@/containers/CreateTournament";

// Bonus Prizes feature has been removed
// This component is kept as a stub to avoid breaking imports
const BonusPrizes = (_props: StepProps) => {
  return null;
};

export default BonusPrizes;
